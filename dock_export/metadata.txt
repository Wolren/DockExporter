[general]
name=Dock Export
description=Spec-driven export of vector and raster layers to single files or GeoPackage, with per-layer renamed export names, QGIS expression filtering, and style management. Never renames live project layers.
about=Exports vector and raster layers to various formats (GeoPackage, Shapefile, GeoJSON, KML, GeoTIFF) with full control over output names via an editable table. Uses a spec-driven architecture: export names are staged per-item and injected only at write time, so the live layer tree is never mutated. Supports per-layer QGIS expression filtering, QML/SLD style export, in-GPKG style embedding, and optional project layer replacement after export.
version=1.0.0
qgisMinimumVersion=3.22
qgisMaximumVersion=4.99
author=dock_export
email=
homepage=
tracker=
repository=
icon=icons/dock_export.svg
tags=export, geopackage, vector, raster, style, filter, dock
experimental=False
deprecated=False
