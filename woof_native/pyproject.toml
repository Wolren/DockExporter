[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "_woof_native"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/abi3-py39"]
module-name = "_native_impl"
